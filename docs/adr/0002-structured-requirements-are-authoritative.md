# Structured requirement definitions are authoritative

Semantic and quantitative applicability requirements are canonical structured definitions, while SHACL engines, R functions, or other executable evaluators are versioned implementation bindings. This keeps a standard independent of implementation language and makes evaluator correctness testable through mandatory valid, invalid, and boundary vectors; all evaluators produce a common conformance, violations, and attestation contract.
